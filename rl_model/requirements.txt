# RL Model Dependencies (in addition to base requirements.txt)
torch>=2.0.0            # PyTorch (install CUDA version: pip install torch --index-url https://download.pytorch.org/whl/cu124)
stable-baselines3>=2.0.0
gymnasium>=0.29.0
tensorboard>=2.14.0     # For training visualization
tqdm>=4.65.0            # Progress bars
